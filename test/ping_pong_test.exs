defmodule ProcessTesting.PingPongTest do
  use ExUnit.Case

  describe "volley_to/1" do
    test "the opponent hits it back to us" do
      flunk("not implemented yet")
    end
  end

  describe "volley_to/2" do
    test "the opponent hits it back to who it's from" do
      flunk("not implemented yet")
    end
  end

  describe "volley_count/1" do
    test "returns how many volleys have been successfully returned by the opponent" do
      flunk("not implemented yet")
    end
  end

  describe "looping volleys" do
    test "two opponents will volley back and forth with each other until one tires out" do
      flunk("not implemented yet")
    end
  end
end
